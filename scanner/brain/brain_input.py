"""Brain input — read truth files, normalize, surface as derived-view inputs.

See doc/brain_design_v1.md §2 + §4 Step 3 for the full read contract.
Step 2 stub: load_truth_files() returns empty dict; Step 3 fills.
"""


def load_truth_files() -> dict:
    """Step 3 will populate. Step 2 stub returns empty dict."""
    return {}
