# .github/workflows/colab_sync.yml
# Purpose: Export signal_history.json as Colab-ready JSON
#          Creates output/colab_export.json with enriched fields
#          Colab notebooks read from GitHub Pages URL directly
# Trigger: GitHub schedule — 4:15 PM IST Mon-Fri (after EOD)
# Sequence: 40 min after eod_master (3:35 PM)
# Touches: output/colab_export.json
# Failure mode: Telegram alert via send_workflow_failure
#
# Architecture v2 (Apr 22 2026):
# - GitHub schedule (drift-tolerant, post-EOD analytics)
# - Standalone for now (may fold into eod_master Step 9 later)
# - 4:15 PM IST after eod_master writes signal_history.json
# - Read-only — pulls latest output/ before exporting

name: "🔄 Colab Sync | Flatten gen1 signals to colab_export.json | 4:15 PM IST Daily"

on:
  schedule:
    # Daily 10:45 AM UTC = 4:15 PM IST (after EOD)
    - cron: '45 10 * * 1-5'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Pull latest output files
        run: |
          git fetch origin
          git checkout origin/main -- output/ || true

      - name: Run colab sync
        env:
          PYTHONPATH: ${{ github.workspace }}:${{ github.workspace }}/scanner
        run: python scanner/colab_sync.py

      - name: Commit export
        run: |
          git config --global user.email "scanner@tietiy"
          git config --global user.name  "TIE TIY Scanner"
          git add output/colab_export.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No export changes"
            exit 0
          fi
          git commit -m "chore: colab export $(date -u +%Y-%m-%d)"
          for i in 1 2 3; do
            git pull --rebase origin main && \
            git push origin HEAD:main && \
            echo "Push succeeded on attempt $i" && break
            echo "Push attempt $i failed — retrying in 10s..."
            sleep 10
          done

      - name: Notify on failure
        if: failure()
        env:
          TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          PYTHONPATH:       ${{ github.workspace }}:${{ github.workspace }}/scanner
        run: |
          python -c "
          import sys; sys.path.insert(0, 'scanner')
          from telegram_bot import send_workflow_failure
          send_workflow_failure('colab_sync', '${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}')
          "
